# multivendor_adaptor_module-4216-4305

This workspace contains the Multivendor Adapter Frontend UI in `FrontendUI/`.

- Start the frontend: cd FrontendUI && npm install && npm start
- Configure API base URL via `FrontendUI/.env` (see `.env.example`).